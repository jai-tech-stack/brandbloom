[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install fastapi uvicorn[standard] python-dotenv pydantic anthropic emergentintegrations"]

[start]
cmd = "uvicorn backend.server:app --host 0.0.0.0 --port $PORT"